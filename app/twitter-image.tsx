// L'image Twitter est identique à l'image OpenGraph (1200×630,
// summary_large_image). On réexporte pour éviter de dupliquer le JSX.
export { default, alt, size, contentType } from "./opengraph-image";
